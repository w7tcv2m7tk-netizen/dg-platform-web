import { AppFeaturePlaceholder } from "@/components/platform/AppFeaturePlaceholder";

export default function AccommodationCalendarPage() {
  return <AppFeaturePlaceholder itemId="accommodation.calendar" />;
}
