import { DomainsConsole } from "@/components/infrastructure/DomainsConsole";

/** DigitalGate Domains — search, register (gated), connect, DNS. */
export default function Page() {
  return <DomainsConsole />;
}
