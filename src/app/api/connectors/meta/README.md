# Meta connector API

Planned production callback: `/api/connectors/meta/callback`.

Implementation must use signed organisation-scoped OAuth state and encrypted per-organisation token storage. See `docs/integrations/meta-api.md` before adding routes.
